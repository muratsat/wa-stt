{
  "name": "wa-stt",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build --compile --minify-whitespace --minify-syntax --targetbun-linux-x64 --outfile build/server src/index.ts"
  },
  "dependencies": {
    "@t3-oss/env-core": "^0.13.10",
    "elysia": "latest",
    "openai": "^6.17.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.9.3"
  },
  "module": "src/index.js"
}
